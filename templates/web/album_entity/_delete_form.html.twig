<form method="post" action="{{ path('app_web_album_entity_delete', {'id': album_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album_entity.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
