{% extends 'base.html.twig' %}

{% block title %}New AlbumEntity{% endblock %}

{% block body %}
    <div class="container p-5">
        <h1>Create a new Album</h1>

        <div class="row">
        <div class="col-12 mb-4">
            <a href="{{ path('app_web_album_entity_index') }}" class="btn btn-secondary mt-3">Back to list</a>
        </div>
        <div class="col-12">
             {{ include('web/album_entity/_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
